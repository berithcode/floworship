{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && npm install && cd apps/api && rm -rf node_modules/.prisma && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd ../.. && cd apps/api && npx prisma migrate deploy && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
